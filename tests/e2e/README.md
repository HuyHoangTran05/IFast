# tests/e2e — Kiểm thử đầu-cuối

Owner: **R4b Quality/Release**.

R4b viết E2E cho `web/` để R1 không phải gánh. Luồng ưu tiên: tìm xe → so sánh
→ đăng ký lái thử → đặt cọc → thanh toán.

R4b cũng giữ release checklist và security review trước mỗi lần lên production.
